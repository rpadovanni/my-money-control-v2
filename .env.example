# Supabase (opcional). Com ambos definidos, o app oferece login e sync online-first.
# Copie para `.env` na raiz do projeto. Nunca commite `.env` com valores reais.
#
# VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
